# Architecture Notes

This document records implementation notes only.

`PROJECT.md` remains the architecture source of truth.

Berlin Demo v0.1 creates a local runtime shell that can be upgraded later without changing the public API.
